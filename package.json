{
  "name": "maison-lunar",
  "version": "1.0.0",
  "description": "Maison Lunar — small-batch fragrance storefront with accounts, orders, verified reviews, and a studio dashboard.",
  "private": true,
  "engines": { "node": ">=22.5.0" },
  "scripts": {
    "start": "node --disable-warning=ExperimentalWarning server.js",
    "dev": "ML_LOG=1 node --disable-warning=ExperimentalWarning server.js",
    "test": "node --disable-warning=ExperimentalWarning test.js && node --disable-warning=ExperimentalWarning test-client.js",
    "test:api": "node --disable-warning=ExperimentalWarning test.js",
    "test:client": "node --disable-warning=ExperimentalWarning test-client.js",
    "backup": "node -e \"const f=require('fs'),p=require('path');const s=p.join('data','maison-lunar.db');const d=p.join('data','backup-'+new Date().toISOString().slice(0,10)+'.db');f.copyFileSync(s,d);console.log('Backed up to '+d)\""
  },
  "dependencies": {}
}
